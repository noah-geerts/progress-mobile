# Verification commands
- Run `npx tsc --noEmit` to check typing
- Run npx expo export if you actually need to make sure the expo project can bundle using Metro

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
